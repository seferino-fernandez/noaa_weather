stations/tafs.json is trimmed. Rule: keep the envelope, keep the first 3 elements of '@graph', keep any later element introducing a key path the kept ones lack, drop the rest.
Captured 697 elements, kept 3, dropped 694.
