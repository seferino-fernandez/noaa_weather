radio/county.json is trimmed. Rule: keep the envelope, keep the first 5 elements of '@graph', keep any later element introducing a key path the kept ones lack, drop the rest.
Captured 128 elements, kept 5, dropped 123.
